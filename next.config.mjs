/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // If you later switch to next/image, add allowed domains here via `images.domains`.
};

export default nextConfig;
