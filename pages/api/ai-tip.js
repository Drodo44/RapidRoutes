// /pages/api/ai-tip.js
export default function handler(req, res) {
  res.status(200).json({
    tip: "For tough markets, stagger your crawl cities for better DAT visibility and carrier reach."
  });
}
